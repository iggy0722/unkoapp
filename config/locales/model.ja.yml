ja:
  activerecord:
    models:
      user: ユーザー # view側： User.model_name.human => "ユーザ" / t("activerecord.models.user")と同じ
      poop: 記録
    attributes:
      user:
        id: ID
        email: メールアドレス
        crypted_password: パスワード
        created_at: 作成日
        updated_at: 更新日
        
